---
description: 销售日志 AI Prompt 变更需同步上线
globs: src/lib/agent/sales-log-prompt.ts,src/lib/agent/**/*.ts,src/components/admin/sales-log-prompt-settings.tsx
alwaysApply: false
---

# 销售日志 Prompt 上线约定

修改 `src/lib/agent/sales-log-prompt.ts`（开场白 `SALES_LOG_OPENING_MESSAGE` 或系统提示词 `SALES_LOG_SYSTEM_PROMPT`），或改会影响对话语气的相关注入文案（如 `today-work-context.ts`）时：

1. **本地改完后，必须提醒用户一并更新线上**，不要只停在本地。
2. **生效方式**
   - 线上「系统默认」（DB 无自定义提示词）：**部署含该改动的版本**后即生效。
   - 线上「已自定义」：仅发版不会覆盖 DB；需在线上「系统配置 → 日志助手提示词」中同步新内容，或「恢复默认」后再发版。
3. 部署前可快速核对线上是否自定义：管理端提示词页「当前状态」徽章。

相关行为约定（已写入默认 Prompt，上线时勿丢）：

- 对销售口语化，禁止「落库」等 IT 黑话
- 打卡/权限自己查，不问「今天打卡了吗」；可写时不念负责人等正常事实
- 每次只追问一个问题
