---
description: 代码更新后自动检查并重启开发服务器
alwaysApply: true
---

# 开发服务器工作流

在本项目中完成**实质性代码变更**（改源码、Prisma schema、依赖、路由等）后，**必须**在回复用户前执行：

## 1. 检查

```bash
cd ~/Projects/hospital-crm-pm
npm run dev:check
```

若变更涉及 `prisma/schema.prisma` 或 migrations，先执行 `npx prisma migrate deploy`（或 `db:migrate:deploy`）。

## 2. 重启 dev 服务器

```bash
npm run dev:restart
```

该脚本会：释放 3000–3003 端口 → `prisma generate` → 删除 `.next` → 类型检查 → 后台启动 dev → curl 验证 `/login` 返回 200。

## 3. 向用户汇报

简要说明检查结果（通过/失败）和服务地址 `http://localhost:3000`。

## 例外

- 仅改文档、注释、plan 文件时不必重启。
- 用户明确说「不要重启」时跳过。
